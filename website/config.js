// Frontend runtime config. API_BASE is the Function App the site calls (cross-origin;
// the Function App's platform CORS must list this site's origin). Remove this file or
// leave API_BASE unset to fall back to same-origin '/api' (the nginx proxy on the VM).
window.API_BASE = 'https://madebytk-api-etfcbdffemf3dcaj.southeastasia-01.azurewebsites.net';
